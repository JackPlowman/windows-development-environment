name: Clean Caches

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

permissions: {}

jobs:
  clean-caches:
    name: Clean Caches
    permissions:
      contents: read
    uses: JackPlowman/reusable-workflows/.github/workflows/common-clean-caches.yml@d14c0675f51e837362b7c522d50f2d0e9947024d # v2025.10.14.02
    secrets:
      workflow_github_token: ${{ secrets.GH_TOKEN }}
